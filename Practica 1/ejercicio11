new Promise((resolve) => resolve(3))
    .then((num) => {
        console.log(num);
        return num*3;
    })
    .then((num) => {
        console.log(num);  
        return num*1+2;
    })
    .then((num) => {
        console.log(num);  
    });
